namespace org.z2legacy.ut.call;

using Foo;

class CallFail05 {
	def test(const a: Foo) {
		a = 0;
		System.Out << "value\n";
	}
	
	def @main() {
		val a = Foo{};
		test(Foo{});
		test(a);
	}
}