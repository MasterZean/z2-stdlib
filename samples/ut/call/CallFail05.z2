using org.z2legacy.samples.ut.Foo;

namespace org.z2legacy.samples.ut;

class CallFail05 {
	def test(const a: Foo) {
		a = 0;
		System.Out << "value\n";
	}
	
	def @main() {
		val a = Foo{};
		test(Foo{});
		test(a);
	}
}