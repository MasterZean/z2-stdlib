namespace org.z2legacy.samples.ut;

class CallPass02 {
	def test(const a: Int) {
		System.Out << "const " << a.class  << " value\n";
	}
	
	def @main() {
		val a = 0;
		test(0);
		test(a);
	}
}