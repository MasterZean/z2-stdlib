namespace org.z2legacy.ut.call;

class CallPass02 {
	def test(const a: Int) {
		System.Out << "const " << a.class  << " value\n";
	}
	
	def @main() {
		val a = 0;
		test(0);
		test(a);
	}
}