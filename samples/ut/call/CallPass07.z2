namespace org.z2legacy.ut.call;

class CallPass07 {
	def test(const a: Small) {
		System.Out << "const " << a.class << " value\n";
	}
	
	def test(ref a: Small) {
		System.Out << a.class << " reference\n";
	}
	
	def @main() {
		val a = Small{};
		test(Small{});
		test(a);
	}
}