namespace org.z2legacy.ut.overloading;

class ByteFail04 {
	def @main() {
		test(Byte.Min);
	}
	
	def test(b: Small) {
		System.Out << b.class;
	}
	
	def test(b: Char) {
		System.Out << b.class;
	}
	
	def test(b: PtrSize) {
		System.Out << b.class;
	}
}