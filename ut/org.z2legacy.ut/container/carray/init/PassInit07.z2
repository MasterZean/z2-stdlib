namespace org.z2legacy.ut.container.carray;

using BuildLog;

class PassInit07 {
	def @main() {
		val temp = BuildLog{};
		
		System.Out << "declare a:\n";
		val a = CArray[BuildLog{}];
		
		System.Out << "declare b:\n";
		val b = CArray[BuildLog.Named{}];
		
		System.Out << "declare c:\n";
		val c = CArray[temp];
	}
}
