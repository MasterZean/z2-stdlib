namespace org.z2legacy.ut.container.vector.init2d;

class PassInit08 {
	def @main() {
		foreach (col, j in [[1, 2, 3, 4], [5, 6, 7], [8, 9]])
			System.Out << "p[" << j << "]: " << col << "\n";
			
		foreach (col, j in [[1, 2, 3, 4], [5, 6, 7], [8, 9]])
			foreach (e, i in col)
				System.Out << "p[" << j << "][" << i << "]=" << e << "\n";
	}
}